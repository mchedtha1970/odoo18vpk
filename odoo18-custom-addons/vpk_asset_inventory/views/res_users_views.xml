<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_users_form_asset_inventory" model="ir.ui.view">
        <field name="name">res.users.form.asset.inventory</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='preferences']" position="inside">
                <group string="ตรวจนับทรัพย์สิน" groups="vpk_asset_inventory.group_asset_inventory_officer">
                    <field name="asset_inventory_analytic_ids" widget="many2many_tags"
                             options="{'no_create': True}"/>
                </group>
            </xpath>
        </field>
    </record>
</odoo>
